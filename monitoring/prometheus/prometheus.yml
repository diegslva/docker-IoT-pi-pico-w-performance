global:
  scrape_interval: 10s
  evaluation_interval: 10s

scrape_configs:
  - job_name: "picodv-server"
    static_configs:
      - targets: ["host.docker.internal:8000"]
        labels:
          service: "picodv-server"
          environment: "dev"
    metrics_path: "/metrics"
